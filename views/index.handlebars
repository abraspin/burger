<header>
    <div class="container">
        <div class="row">
            <img class="mx-auto" id="burger-image" src="./public/assets/img/burger.png" alt="burger-image">

        </div>
        <div class="row">
            <h1 class="mx-auto ">Eat Da Burger!</h1>
        </div>
    </div>
</header>

<hr>
<div class="container">
    <div class="row ">

        <!-- <div id="burgers-to-devour" class="col-md-5 border"> -->
        <div id="burgers-to-devour" class="col-md-6 border">

            <h2 class="text-center">Burgers to be Devoured!</h2>

            <br>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                {{!-- {{> burgers/burger-block devoured=false}} --}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>

        <!-- <div class="col-md-2 "></div> -->
        <!-- <div id="devoured-burgers" class="col-md-5 border"> -->
        <div id="devoured-burgers" class="col-md-6 border">
            <h2 class="text-center">Devoured Burgers!</h2>

            <br>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                {{!-- {{> burgers/burger-block devoured=false}} --}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>

    <br>

    <div class="container">
        <div class="row">

            <h3 class="mx-auto">Add a Burger</h3>
        </div>
        <br>
        <div class="row">
            <form class=" mx-auto create-form">

                <!-- MAY NEED TO UPDATE THESE IDS AND VARS  -->
                <div class="form-group">
                    <label for="ca">Burger Name:</label>
                    <input type="text" id="ca" name="burger_name">
                    <button id="add-burger-btn" type="submit">Add da Burger!</button>
                </div>

                <!-- <div class="form-group">
                <label for="devoured">Devoured?</label><br>
                <input type="radio" name="devoured" value="1" checked> Devoured!<br>
                <input type="radio" name="devoured" value="0"> To Devour!
            </div>

            <button type="submit">Eat Da Burger!</button> -->
            </form>

        </div>
    </div>