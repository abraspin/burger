<li class="mx-auto my-1 px-3 py-2 btn-primary rounded border border-info">
    <span style="font-size: 1.5rem;"> {{burger_name}}</span>
    {{#if devoured}} {{else}}<button class="devour-btn btn btn-secondary mx-2" data-burger-name="{{burger_name}}"
        type="submit">Devour</button>{{/if}}
    <button class="trash-btn btn btn-danger mx-2" data-burger-name="{{burger_name}}" type="submit"><i
            class='fas fa-trash fa-lg' style='size:36px'></i>
    </button>
    {{!-- style='size:36px' --}}
</li>